{
  "name": "robot-visual-perception-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "echo 'Add ESLint when you add code'",
    "build": "mkdir -p dist && cp -r src/* dist/",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "2.1.5",
    "prettier": "3.4.2"
  }
}
